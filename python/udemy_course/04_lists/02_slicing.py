# create a list of name
my_list:list[str] = ["huzair","huzaifa","khizar"]

# slice form index 0,1 - only remove first elemnt
print(my_list[0:1])

# remove all elements except last elemnt
print(my_list[-1:])

# remove middle elemnt only
mid_index = len(my_list)
print(mid_index //2)

